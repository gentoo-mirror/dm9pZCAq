<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>v@0x0c.link</email>
		<name>dm9pZCAq</name>
	</maintainer>
	<use>
		<flag name="path">
			Insert uutils PATH before baselayout PATH to use uutils instead of coreutils
		</flag>
		<flag name="uu_progs_base32">Build the base32 program</flag>
		<flag name="uu_progs_base64">Build the base64 program</flag>
		<flag name="uu_progs_basename">Build the basename program</flag>
		<flag name="uu_progs_cat">Build the cat program</flag>
		<flag name="uu_progs_cksum">Build the cksum program</flag>
		<flag name="uu_progs_comm">Build the comm program</flag>
		<flag name="uu_progs_cp">Build the cp program</flag>
		<flag name="uu_progs_csplit">Build the csplit program</flag>
		<flag name="uu_progs_cut">Build the cut program</flag>
		<flag name="uu_progs_date">Build the date program</flag>
		<flag name="uu_progs_df">Build the df program</flag>
		<flag name="uu_progs_dircolors">Build the dircolors program</flag>
		<flag name="uu_progs_dirname">Build the dirname program</flag>
		<flag name="uu_progs_echo">Build the echo program</flag>
		<flag name="uu_progs_env">Build the env program</flag>
		<flag name="uu_progs_expand">Build the expand program</flag>
		<flag name="uu_progs_expr">Build the expr program</flag>
		<flag name="uu_progs_factor">Build the factor program</flag>
		<flag name="uu_progs_false">Build the false program</flag>
		<flag name="uu_progs_fmt">Build the fmt program</flag>
		<flag name="uu_progs_fold">Build the fold program</flag>
		<flag name="uu_progs_hashsum">Build the hashsum program</flag>
		<flag name="uu_progs_head">Build the head program</flag>
		<flag name="uu_progs_join">Build the join program</flag>
		<flag name="uu_progs_link">Build the link program</flag>
		<flag name="uu_progs_ln">Build the ln program</flag>
		<flag name="uu_progs_ls">Build the ls program</flag>
		<flag name="uu_progs_mkdir">Build the mkdir program</flag>
		<flag name="uu_progs_mktemp">Build the mktemp program</flag>
		<flag name="uu_progs_more">Build the more program</flag>
		<flag name="uu_progs_mv">Build the mv program</flag>
		<flag name="uu_progs_nl">Build the nl program</flag>
		<flag name="uu_progs_nproc">Build the nproc program</flag>
		<flag name="uu_progs_numfmt">Build the numfmt program</flag>
		<flag name="uu_progs_od">Build the od program</flag>
		<flag name="uu_progs_paste">Build the paste program</flag>
		<flag name="uu_progs_printenv">Build the printenv program</flag>
		<flag name="uu_progs_printf">Build the printf program</flag>
		<flag name="uu_progs_ptx">Build the ptx program</flag>
		<flag name="uu_progs_pwd">Build the pwd program</flag>
		<flag name="uu_progs_readlink">Build the readlink program</flag>
		<flag name="uu_progs_realpath">Build the realpath program</flag>
		<flag name="uu_progs_relpath">Build the relpath program</flag>
		<flag name="uu_progs_rm">Build the rm program</flag>
		<flag name="uu_progs_rmdir">Build the rmdir program</flag>
		<flag name="uu_progs_seq">Build the seq program</flag>
		<flag name="uu_progs_shred">Build the shred program</flag>
		<flag name="uu_progs_shuf">Build the shuf program</flag>
		<flag name="uu_progs_sleep">Build the sleep program</flag>
		<flag name="uu_progs_sort">Build the sort program</flag>
		<flag name="uu_progs_split">Build the split program</flag>
		<flag name="uu_progs_sum">Build the sum program</flag>
		<flag name="uu_progs_sync">Build the sync program</flag>
		<flag name="uu_progs_tac">Build the tac program</flag>
		<flag name="uu_progs_tail">Build the tail program</flag>
		<flag name="uu_progs_tee">Build the tee program</flag>
		<flag name="uu_progs_test">Build the test program</flag>
		<flag name="uu_progs_tr">Build the tr program</flag>
		<flag name="uu_progs_true">Build the true program</flag>
		<flag name="uu_progs_truncate">Build the truncate program</flag>
		<flag name="uu_progs_tsort">Build the tsort program</flag>
		<flag name="uu_progs_unexpand">Build the unexpand program</flag>
		<flag name="uu_progs_uniq">Build the uniq program</flag>
		<flag name="uu_progs_wc">Build the wc program</flag>
		<flag name="uu_progs_whoami">Build the whoami program</flag>
		<flag name="uu_progs_yes">Build the yes program</flag>
		<flag name="uu_unix_progs_arch">Build the arch program</flag>
		<flag name="uu_unix_progs_chgrp">Build the chgrp program</flag>
		<flag name="uu_unix_progs_chmod">Build the chmod program</flag>
		<flag name="uu_unix_progs_chown">Build the chown program</flag>
		<flag name="uu_unix_progs_chroot">Build the chroot program</flag>
		<flag name="uu_unix_progs_du">Build the du program</flag>
		<flag name="uu_unix_progs_groups">Build the groups program</flag>
		<flag name="uu_unix_progs_hostid">Build the hostid program</flag>
		<flag name="uu_unix_progs_hostname">Build the hostname program</flag>
		<flag name="uu_unix_progs_id">Build the id program</flag>
		<flag name="uu_unix_progs_install">Build the install program</flag>
		<flag name="uu_unix_progs_kill">Build the kill program</flag>
		<flag name="uu_unix_progs_logname">Build the logname program</flag>
		<flag name="uu_unix_progs_mkfifo">Build the mkfifo program</flag>
		<flag name="uu_unix_progs_mknod">Build the mknod program</flag>
		<flag name="uu_unix_progs_nice">Build the nice program</flag>
		<flag name="uu_unix_progs_nohup">Build the nohup program</flag>
		<flag name="uu_unix_progs_pathchk">Build the pathchk program</flag>
		<flag name="uu_unix_progs_pinky">Build the pinky program</flag>
		<flag name="uu_unix_progs_stat">Build the stat program</flag>
		<flag name="uu_unix_progs_stdbuf">Build the stdbuf program</flag>
		<flag name="uu_unix_progs_timeout">Build the timeout program</flag>
		<flag name="uu_unix_progs_touch">Build the touch program</flag>
		<flag name="uu_unix_progs_tty">Build the tty program</flag>
		<flag name="uu_unix_progs_uname">Build the uname program</flag>
		<flag name="uu_unix_progs_unlink">Build the unlink program</flag>
		<flag name="uu_unix_progs_uptime">Build the uptime program</flag>
		<flag name="uu_unix_progs_users">Build the users program</flag>
		<flag name="uu_unix_progs_who">Build the who program</flag>
	</use>
	<upstream>
		<bugs-to>https://github.com/uutils/coreutils</bugs-to>
		<remote-id type="github">uutils/coreutils</remote-id>
	</upstream>
</pkgmetadata>
